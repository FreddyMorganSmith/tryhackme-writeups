# Key commands used:

# Run a directory brute-force scan
dirb http://fakebank.thm

# After dirb reports a found path, open it in a browser:
# Example (paste into your browser address bar):
# http://fakebank.thm/<discovered-path>
